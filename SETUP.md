# 🚀 SETUP: Готов к разработке

> **Статус:** Все требования согласованы ✅  
> **Дата:** 16 февраля 2026  
> **Шаблон:** Vite (пустой, готов к работе)

---

## ✅ Принятые технические решения

### 🎨 Стилизация
- **CSS Modules** (встроено в Vite, настройка не требуется)
- Файлы: `Component.module.css`
- Использование: `import styles from './Component.module.css'`

### 📦 Зависимости

**Обязательные библиотеки:**
```bash
npm install react-router-dom date-fns uuid
```

**Детально:**
- `react-router-dom` — навигация (добавим позже, но установить сейчас)
- `date-fns` — работа с датами (форматирование времени занятий)
- `uuid` — генерация уникальных ID для сообщений

### 🔧 Управление состоянием
- **Только React hooks:** `useState`, `useEffect`
- **Без:** Context API, Redux, Zustand
- **Простота:** Локальное состояние в компонентах

---

## 🎯 Функциональные требования MVP

### 1️⃣ Архитектура
- ✅ **Одностраничное приложение** (SPA без роутинга в MVP)
- ✅ **Все на главной странице** (чат + результаты)
- 🔮 **Роутинг добавим позже** (библиотека уже установлена)

### 2️⃣ Основной функционал

**Обязательно в MVP:**
- ✅ Чат-интерфейс (ввод → обработка → вывод)
- ✅ История сообщений (хранить в памяти во время сессии)
- ✅ Отображение расписания с оценками нечёткой логики
- ✅ Парсинг команд пользователя

**Дополнительные фичи:**
- ✅ **Быстрые кнопки** с примерами команд (под полем ввода)
- ✅ **Автодополнение** при вводе команд (выпадающий список)
- ✅ **Темная/светлая тема** (переключатель в шапке)

### 3️⃣ Приоритет разработки

```
ЭТАП 1: Функционал (приоритет)
├── Базовый чат (ввод/вывод)
├── Интеграция с сервисами
├── Отображение расписания
└── Работа нечёткой логики

ЭТАП 2: Улучшения
├── Быстрые кнопки
├── Автодополнение
├── История сообщений
└── Темная тема

ЭТАП 3: Красота (финал)
├── Полировка стилей
├── Адаптивность
├── Анимации (если время)
└── Доработка UX
```

---

## 📁 Структура проекта (целевая)

```
src/
├── components/
│   ├── Chat/
│   │   ├── Chat.jsx                    # Главный контейнер чата
│   │   ├── Chat.module.css
│   │   ├── MessageList.jsx             # Список сообщений
│   │   ├── MessageList.module.css
│   │   ├── Message.jsx                 # Одно сообщение (user/bot)
│   │   ├── Message.module.css
│   │   ├── InputField.jsx              # Поле ввода + кнопка
│   │   ├── InputField.module.css
│   │   ├── QuickButtons.jsx            # Быстрые кнопки команд
│   │   ├── QuickButtons.module.css
│   │   └── Autocomplete.jsx            # Автодополнение команд
│   │       └── Autocomplete.module.css
│   │
│   ├── Schedule/
│   │   ├── ScheduleList.jsx            # Список занятий
│   │   ├── ScheduleList.module.css
│   │   ├── ScheduleCard.jsx            # Карточка одного занятия
│   │   └── ScheduleCard.module.css
│   │
│   ├── Layout/
│   │   ├── Header.jsx                  # Шапка с темой
│   │   ├── Header.module.css
│   │   └── ThemeToggle.jsx             # Переключатель темы
│   │       └── ThemeToggle.module.css
│   │
│   └── Common/
│       ├── Loader.jsx                  # Индикатор загрузки
│       ├── Loader.module.css
│       ├── ErrorMessage.jsx            # Сообщение об ошибке
│       └── ErrorMessage.module.css
│
├── services/                           # ✅ УЖЕ ГОТОВО
│   ├── fuzzyLogic.js                   # Нечёткая логика (3 модели)
│   ├── scheduleService.js              # Работа с расписанием
│   └── commandParser.js                # Парсинг команд
│
├── data/                               # ✅ УЖЕ ГОТОВО
│   └── scheduleData.js                 # Примеры расписания
│
├── hooks/                              # Кастомные хуки
│   ├── useChat.js                      # Логика чата
│   ├── useTheme.js                     # Управление темой
│   └── useAutocomplete.js              # Логика автодополнения
│
├── utils/                              # Вспомогательные функции
│   ├── formatters.js                   # Форматирование (даты, время)
│   ├── constants.js                    # Константы (цвета, команды)
│   └── helpers.js                      # Общие хелперы
│
├── styles/
│   ├── variables.css                   # CSS переменные (цвета, шрифты)
│   ├── global.css                      # Глобальные стили
│   └── themes.css                      # Темная/светлая тема
│
├── App.jsx                             # Главный компонент
├── App.module.css
├── main.jsx                            # Точка входа
└── index.css                           # Базовые стили
```

---

## 🎨 Дизайн-система (кратко)

### Цветовая схема

**Светлая тема:**
```css
--background: #f5f5f5;
--surface: #ffffff;
--primary: #2196F3;
--text-primary: #212121;
--text-secondary: #757575;
--border: #e0e0e0;
```

**Темная тема:**
```css
--background: #121212;
--surface: #1e1e1e;
--primary: #64B5F6;
--text-primary: #ffffff;
--text-secondary: #b0b0b0;
--border: #333333;
```

**Оценки нечёткой логики:**
```css
--score-excellent: #4CAF50;  /* 8-10 */
--score-good: #8BC34A;       /* 6-8 */
--score-neutral: #FFC107;    /* 4-6 */
--score-poor: #FF5722;       /* 0-4 */

--priority-high: #4CAF50;    /* 🟢 */
--priority-medium: #FFC107;  /* 🟡 */
--priority-low: #FF5722;     /* 🔴 */
```

### Типографика
```css
--font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
--font-size-base: 16px;
--font-size-small: 14px;
--font-size-large: 18px;
--font-size-xlarge: 24px;
```

### Отступы
```css
--spacing-xs: 4px;
--spacing-sm: 8px;
--spacing-md: 16px;
--spacing-lg: 24px;
--spacing-xl: 32px;
```

---

## 💬 Формат сообщений в чате

### Структура данных

```javascript
// Сообщение пользователя
{
  id: 'uuid',
  type: 'user',
  text: 'расписание ПИ-301',
  timestamp: Date.now()
}

// Текстовое сообщение бота
{
  id: 'uuid',
  type: 'bot',
  text: 'Привет! Чем могу помочь?',
  timestamp: Date.now()
}

// Сообщение с расписанием
{
  id: 'uuid',
  type: 'bot',
  contentType: 'schedule',
  data: {
    group: { name: 'ПИ-301', ... },
    schedule: [...], // с fuzzyEvaluation
    recommendations: [...]
  },
  timestamp: Date.now()
}

// Сообщение об ошибке
{
  id: 'uuid',
  type: 'bot',
  contentType: 'error',
  text: 'Группа не найдена',
  timestamp: Date.now()
}
```

---

## 🎯 Примеры команд для автодополнения

```javascript
const commandSuggestions = [
  {
    command: 'расписание группа ПИ-301',
    description: 'Показать расписание группы',
    icon: '📅'
  },
  {
    command: 'расписание преподаватель Иванова',
    description: 'Расписание преподавателя',
    icon: '👨‍🏫'
  },
  {
    command: 'расписание ПИ-301 в понедельник',
    description: 'Расписание на день',
    icon: '📆'
  },
  {
    command: 'когда свободен Петров в четверг',
    description: 'Свободные окна преподавателя',
    icon: '🕐'
  },
  {
    command: 'список групп',
    description: 'Все доступные группы',
    icon: '📋'
  },
  {
    command: 'список преподавателей',
    description: 'Все преподаватели',
    icon: '👥'
  },
  {
    command: 'рекомендации для ПИ-301',
    description: 'Анализ расписания с советами',
    icon: '💡'
  },
  {
    command: 'помощь',
    description: 'Показать список команд',
    icon: '❓'
  }
];
```

---

## 📝 Быстрые кнопки (QuickButtons)

Отображаются под полем ввода для новых пользователей:

```javascript
const quickButtons = [
  { label: '📅 Расписание ПИ-301', command: 'расписание группа ПИ-301' },
  { label: '👨‍🏫 Преподаватели', command: 'список преподавателей' },
  { label: '💡 Рекомендации', command: 'рекомендации для ПИ-301' },
  { label: '❓ Помощь', command: 'помощь' }
];
```

Клик по кнопке → автоматически вставляет команду в поле ввода.

---

## 🔄 Логика работы (псевдокод)

### Основной флоу чата

```javascript
// Chat.jsx
function Chat() {
  const [messages, setMessages] = useState([]);
  const [inputValue, setInputValue] = useState('');
  const [theme, setTheme] = useState('light');

  const handleSend = async (text) => {
    // 1. Добавить сообщение пользователя
    const userMessage = {
      id: uuid(),
      type: 'user',
      text,
      timestamp: Date.now()
    };
    setMessages(prev => [...prev, userMessage]);

    // 2. Парсить команду
    const command = commandParser.parse(text);

    // 3. Обработать команду
    const botResponse = await handleCommand(command);

    // 4. Добавить ответ бота
    setMessages(prev => [...prev, botResponse]);
  };

  const handleCommand = async (command) => {
    switch (command.type) {
      case 'GROUP_SCHEDULE':
        const schedule = scheduleService.getGroupSchedule(command.groupName);
        if (schedule) {
          return {
            id: uuid(),
            type: 'bot',
            contentType: 'schedule',
            data: schedule,
            timestamp: Date.now()
          };
        }
        return createErrorMessage('Группа не найдена');

      case 'TEACHER_SCHEDULE':
        // Аналогично
        break;

      case 'HELP':
        return createHelpMessage();

      case 'UNKNOWN':
        return createErrorMessage('Команда не распознана. Напишите "помощь"');
    }
  };

  return (
    <div className={styles.chat} data-theme={theme}>
      <Header theme={theme} onThemeToggle={setTheme} />
      <MessageList messages={messages} />
      <InputField
        value={inputValue}
        onChange={setInputValue}
        onSend={handleSend}
      />
      <QuickButtons onSelect={(cmd) => setInputValue(cmd)} />
    </div>
  );
}
```

---

## 🎨 Отображение расписания

### ScheduleCard с оценками

```jsx
<ScheduleCard lesson={lesson}>
  <div className={styles.time}>{lesson.time}</div>
  <div className={styles.subject}>{lesson.subject}</div>
  <div className={styles.teacher}>{lesson.teacher}</div>
  <div className={styles.room}>
    📍 {lesson.building}, ауд. {lesson.room}
  </div>
  
  {/* Оценка нечёткой логики */}
  <div
    className={styles.fuzzyScore}
    style={{ color: getScoreColor(lesson.fuzzyEvaluation.score) }}
  >
    <span className={styles.label}>
      {lesson.fuzzyEvaluation.label}
    </span>
    <span className={styles.score}>
      {lesson.fuzzyEvaluation.score}/10
    </span>
  </div>
  
  <div className={styles.advice}>
    💡 {lesson.fuzzyEvaluation.advice}
  </div>
</ScheduleCard>
```

---

## 🚀 Пошаговый план разработки

### Этап 1: Базовая структура (1-2 часа)
1. Установить зависимости
2. Скопировать services/ и data/
3. Настроить CSS переменные и темы
4. Создать основные компоненты (пустые)

### Этап 2: Минимальный чат (2-3 часа)
1. Реализовать Chat.jsx с состоянием
2. Создать MessageList и Message
3. Создать InputField
4. Подключить commandParser
5. Базовая обработка команд

### Этап 3: Отображение расписания (2-3 часа)
1. Создать ScheduleCard
2. Создать ScheduleList
3. Интегрировать scheduleService
4. Отобразить оценки fuzzy logic

### Этап 4: Дополнительные фичи (2-4 часа)
1. QuickButtons
2. Autocomplete
3. История сообщений
4. ThemeToggle

### Этап 5: Полировка (1-2 часа)
1. Улучшение стилей
2. Адаптивность
3. Баг-фиксы
4. Финальное тестирование

**Итого:** 8-14 часов чистой разработки

---

## 📦 Команды для старта

```bash
# Установить зависимости
npm install react-router-dom date-fns uuid

# Скопировать готовые модули (если еще не скопированы)
# cp -r ../schedule-chatbot/src/services ./src/
# cp -r ../schedule-chatbot/src/data ./src/

# Запуск dev-сервера
npm run dev

# Тестирование нечёткой логики (отдельно)
# node ../schedule-chatbot/demo-standalone.js
```

---

## ⚠️ Важные замечания

### Что НЕ делать:
- ❌ Не менять fuzzyLogic.js (уже работает!)
- ❌ Не менять scheduleService.js (готов к использованию)
- ❌ Не добавлять роутинг в MVP (одна страница)
- ❌ Не делать сложный state management
- ❌ Не тратить много времени на анимации

### Что делать:
- ✅ Использовать существующие сервисы
- ✅ Начать с простого UI
- ✅ Постепенно добавлять фичи
- ✅ Тестировать на каждом этапе
- ✅ Коммитить часто

---

## 📚 Полезные файлы для справки

- **PROJECT_BRIEF.md** — полное описание проекта
- **CHEATSHEET.md** — шпаргалка по проекту
- **README.md** — документация нечёткой логики
- **demo-standalone.js** — демонстрация работы
- **CLAUDE_CODE_PROMPT.md** — промпты для старта

---

## ✅ Чек-лист перед началом

- [ ] Vite проект создан
- [ ] Зависимости установлены (`react-router-dom`, `date-fns`, `uuid`)
- [ ] Папки `services/` и `data/` скопированы в `src/`
- [ ] `demo-standalone.js` запущен и работает
- [ ] Структура папок создана
- [ ] CSS переменные настроены
- [ ] Темная/светлая тема работает

---

**Статус:** 🟢 Готов к разработке!

**Команда для старта в Claude Code:**
```
Прочитай файлы SETUP.md и PROJECT_BRIEF.md. 
Начни с создания базовой структуры чата согласно плану.
Используй существующие сервисы из src/services/.
Приоритет: функционал → фичи → красота.
```

---

_Последнее обновление: 16 февраля 2026_
