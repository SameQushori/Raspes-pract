# 📝 ШПАРГАЛКА ПО ПРОЕКТУ

## 🎯 Что уже сделано

### ✅ Нечёткая логика (3 модуля)

1. **TimeConvenienceEvaluator** — оценка времени занятий
2. **TeacherAvailabilityEvaluator** — доступность преподавателя
3. **DayWorkloadEvaluator** — загруженность дня

**Файл:** `src/services/fuzzyLogic.js`

### ✅ Сервисы

- **scheduleService.js** — работа с расписанием
- **commandParser.js** — парсинг команд пользователя

### ✅ Данные

- **scheduleData.js** — примеры расписания групп и преподавателей

### ✅ Демонстрация

- **demo-standalone.js** — запускаемая демонстрация всех функций

## 🚀 Что делать дальше

### Шаг 1: Замените данные расписания

Откройте `src/data/scheduleData.js` и замените примеры на данные вашего колледжа.

**Где взять данные:**
1. Сайт колледжа → парсинг (я помогу)
2. Ручной ввод (10-20 занятий достаточно для демо)
3. Попросить у методиста Excel файл

### Шаг 2: Создайте React интерфейс

Нужны компоненты:
- `Chat.jsx` — главный контейнер чата
- `MessageList.jsx` — список сообщений
- `Message.jsx` — одно сообщение
- `InputField.jsx` — поле ввода
- `ScheduleCard.jsx` — карточка занятия

### Шаг 3: Соедините всё вместе

```javascript
// В компоненте Chat.jsx
import commandParser from '../services/commandParser';
import scheduleService from '../services/scheduleService';

const handleUserMessage = (text) => {
  const command = commandParser.parse(text);
  
  switch (command.type) {
    case 'GROUP_SCHEDULE':
      const schedule = scheduleService.getGroupSchedule(command.groupName);
      // Отобразить расписание
      break;
    
    case 'TEACHER_SCHEDULE':
      const teacher = scheduleService.getTeacherSchedule(command.teacherName);
      // Отобразить расписание преподавателя
      break;
    
    // ... другие команды
  }
};
```

## 📋 Команды для тестирования

Протестируйте эти команды в боте:

```
✅ "расписание группа ПИ-301"
✅ "расписание преподаватель Иванова"
✅ "расписание ПИ-301 в понедельник"
✅ "когда свободен Петров в четверг"
✅ "список групп"
✅ "список преподавателей"
✅ "рекомендации для ПИ-301"
✅ "помощь"
```

## 🧠 Для отчёта: Объяснение нечёткой логики

### Модель 1: Удобство времени

**Простыми словами:**
"Чем ближе к обеду (11:00-14:00), тем удобнее время для занятий. Очень ранние (до 9:00) и поздние (после 16:00) пары менее удобны."

**Функции принадлежности:**
```
РАНО:      |\___        (7:00 = 100%, 10:00 = 0%)
ОПТИМУМ:   __/‾\__      (10:00-14:00 = 100%)
ПОЗДНО:      ___/|      (15:00 = 0%, 18:00 = 100%)
```

**Пример правила:**
```
ЕСЛИ hour >= 10 AND hour <= 14
  ТО удобство = ВЫСОКОЕ (оценка 8-10)
```

### Модель 2: Доступность преподавателя

**Простыми словами:**
"Если у преподавателя мало пар и много свободных часов, он более доступен для консультаций."

**Пример:**
- 2 пары + 5 свободных часов = 🟢 Очень доступен (9/10)
- 6 пар + 1 свободный час = 🔴 Занят (3/10)

### Модель 3: Загруженность дня

**Простыми словами:**
"Чем больше пар и часов занятий, тем более загружен день."

**Формула (Сугэно):**
```
Оценка = (μ_лёгкий×3 + μ_обычный×6 + μ_тяжёлый×9) / Σμ
```

**Пример:**
- 2 пары, 3 часа = 😊 Лёгкий день (3.7/10)
- 6 пар, 9 часов = 😓 Загруженный день (8.3/10)

## 🎨 Советы по UI

### Цветовая схема для оценок

```css
/* Для оценок времени */
.excellent { color: #4CAF50; } /* 8-10 */
.good { color: #8BC34A; }      /* 6-8 */
.neutral { color: #FFC107; }   /* 4-6 */
.poor { color: #FF5722; }      /* 0-4 */
```

### Эмодзи для визуализации

```
⭐ Отличное время
👍 Хорошее время
🌅 Раннее время
🌙 Позднее время
😊 Лёгкий день
📚 Обычный день
😓 Загруженный день
🟢 Высокий приоритет
🟡 Средний приоритет
🔴 Низкий приоритет
```

## 🐛 Как дебажить

### Проверка нечёткой логики

```bash
node demo-standalone.js
```

### Проверка парсинга команд

```javascript
import commandParser from './services/commandParser';

console.log(commandParser.parse('расписание ПИ-301'));
// { type: 'GROUP_SCHEDULE', groupName: 'ПИ-301', confidence: 0.9 }
```

### Проверка сервиса расписания

```javascript
import scheduleService from './services/scheduleService';

const schedule = scheduleService.getGroupSchedule('ПИ-301');
console.log(schedule);
```

## 📊 Что показать на защите

1. **Демонстрация работы бота** (5 мин)
   - Показать разные команды
   - Подчеркнуть работу нечёткой логики

2. **Объяснение нечёткой логики** (3 мин)
   - Показать графики функций принадлежности
   - Привести пример расчёта

3. **Архитектура** (2 мин)
   - Показать структуру проекта
   - Объяснить компоненты

4. **Вопросы** (5 мин)
   - Будьте готовы объяснить выбор функций принадлежности
   - Почему Мамдани vs Сугэно

## ⚡ Быстрый старт для React

```bash
# 1. Создать React приложение
npx create-react-app schedule-chatbot-ui

# 2. Скопировать файлы
cp -r src/services schedule-chatbot-ui/src/
cp -r src/data schedule-chatbot-ui/src/

# 3. Установить зависимости (если нужны)
cd schedule-chatbot-ui
npm install

# 4. Запустить
npm start
```

## 📞 Куда обращаться за помощью

- **Нечёткая логика не работает?** → Проверьте demo-standalone.js
- **Команды не парсятся?** → Проверьте commandParser.js
- **Нет данных расписания?** → Скажите, помогу спарсить с сайта
- **Не знаете как сделать UI?** → Начните с простого списка сообщений

---

💡 **Главное:** У вас уже есть рабочая нечёткая логика! Это 70% проекта. Осталось только обернуть в красивый UI.

🎯 **Следующий шаг:** Скажите, нужна ли помощь с:
1. Парсингом данных с сайта колледжа
2. Созданием React компонентов
3. Чем-то ещё?
