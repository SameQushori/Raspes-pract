# 📋 PROJECT BRIEF: Чат-бот расписания университета

> Документ для передачи контекста проекта в Claude Code

## 🎯 Цель проекта

Разработать мини-чат-бот для консультаций по расписанию университета с применением **нечёткой логики** для учебной практики по модулю ПМ.03.

## 👤 Информация о разработчике

- **Опыт:** Junior Frontend Developer
- **Знания:** React, JavaScript, базовый HTML/CSS
- **Слабые стороны:** Backend разработка (минимальный опыт)
- **Учебное заведение:** Колледж (данные расписания будут реальные)

## ✅ Что уже реализовано

### 1. Нечёткая логика (100% готово)

Файл: `src/services/fuzzyLogic.js`

**3 экспертные системы:**

1. **TimeConvenienceEvaluator** — Оценка удобства времени занятий
   - Модель: Мамдани
   - Входы: hour (7-20), dayOfWeek
   - Выходы: score (0-10), label, advice
   - Функции принадлежности: early, optimal, late
   - 9 правил

2. **TeacherAvailabilityEvaluator** — Доступность преподавателя
   - Модель: Мамдани
   - Входы: lessonCount (0-8), freeSlots (0-8)
   - Выходы: priority (0-10), recommendation
   - Функции принадлежности: lowLoad, mediumLoad, highLoad, highAvail, lowAvail
   - 5 правил

3. **DayWorkloadEvaluator** — Загруженность дня
   - Модель: Сугэно
   - Входы: lessonCount, totalHours, breakTime
   - Выходы: score (0-10), label, advice
   - Дефаззификация: центр тяжести

**Все модули протестированы и работают!** (см. demo-standalone.js)

### 2. Сервисы (готовы к использованию)

- **scheduleService.js** — работа с расписанием, интеграция с fuzzy logic
- **commandParser.js** — парсинг команд пользователя (естественный язык)

### 3. Данные

- **scheduleData.js** — примеры расписания (2 группы, 3 преподавателя)
- Структура: groups[], teachers[], metadata

## 🎯 Текущая задача

**Создать React приложение с UI чат-бота**, который использует существующую логику.

### Требования к UI:

1. **Минималистичный интерфейс чата**
   - Список сообщений (прокрутка)
   - Поле ввода с кнопкой отправки
   - Разделение сообщений (пользователь/бот)

2. **Отображение расписания**
   - Карточки занятий с информацией
   - Визуализация оценок нечёткой логики (цвет/эмодзи)
   - Красивое форматирование времени

3. **Адаптивность**
   - Мобильная версия
   - Десктопная версия

### Стек:

- **Frontend:** React 18+ (Create React App или Vite)
- **Стилизация:** CSS Modules или styled-components (на выбор разработчика)
- **Состояние:** React hooks (useState, useEffect)
- **Никакого backend пока** — всё на клиенте

## 📁 Структура проекта

```
schedule-chatbot/
├── public/
├── src/
│   ├── components/
│   │   ├── Chat/
│   │   │   ├── Chat.jsx              # Главный контейнер
│   │   │   ├── MessageList.jsx       # Список сообщений
│   │   │   ├── Message.jsx           # Одно сообщение
│   │   │   ├── InputField.jsx        # Поле ввода
│   │   │   └── Chat.module.css
│   │   └── Schedule/
│   │       ├── ScheduleCard.jsx      # Карточка занятия
│   │       ├── ScheduleList.jsx      # Список занятий
│   │       └── Schedule.module.css
│   ├── services/
│   │   ├── fuzzyLogic.js      ✅ ГОТОВО
│   │   ├── scheduleService.js ✅ ГОТОВО
│   │   └── commandParser.js   ✅ ГОТОВО
│   ├── data/
│   │   └── scheduleData.js    ✅ ГОТОВО
│   ├── App.jsx
│   ├── App.css
│   └── index.js
├── package.json
└── README.md
```

## 💬 Примеры команд бота

Пользователь должен уметь вводить:

```
✅ "расписание группа ПИ-301"
✅ "расписание преподаватель Иванова"
✅ "расписание ПИ-301 в понедельник"
✅ "когда свободен Петров в четверг"
✅ "список групп"
✅ "список преподавателей"
✅ "рекомендации для ПИ-301"
✅ "помощь"
```

## 🎨 Дизайн-референсы

### Цветовая схема для оценок:

```javascript
// Оценка времени
score >= 8: green (#4CAF50) + ⭐
score >= 6: light-green (#8BC34A) + 👍
score >= 4: orange (#FFC107) + 😐
score < 4: red (#FF5722) + 🌅/🌙

// Приоритет преподавателя
priority >= 8: 🟢 green
priority >= 5: 🟡 yellow
priority < 5: 🔴 red

// Загруженность дня
score < 4: 😊 light
score < 7: 📚 normal
score >= 7: 😓 heavy
```

### UI стиль:

- Чистый, современный интерфейс
- Скруглённые углы для карточек
- Тени для глубины
- Анимации появления сообщений (опционально)

## 🔄 Логика работы чата

```javascript
// Псевдокод
function handleUserMessage(text) {
  // 1. Показать сообщение пользователя
  addMessage({ type: 'user', text });
  
  // 2. Парсить команду
  const command = commandParser.parse(text);
  
  // 3. Обработать команду
  switch (command.type) {
    case 'GROUP_SCHEDULE':
      const schedule = scheduleService.getGroupSchedule(command.groupName);
      if (schedule) {
        // Показать расписание с оценками fuzzy logic
        addMessage({ 
          type: 'bot', 
          content: <ScheduleList schedule={schedule} />
        });
      } else {
        addMessage({ 
          type: 'bot', 
          text: 'Группа не найдена' 
        });
      }
      break;
    
    case 'TEACHER_SCHEDULE':
      // Аналогично
      break;
    
    case 'HELP':
      addMessage({ 
        type: 'bot', 
        text: 'Список команд...' 
      });
      break;
    
    case 'UNKNOWN':
      addMessage({ 
        type: 'bot', 
        text: 'Не понял команду. Напишите "помощь"' 
      });
      break;
  }
}
```

## 📝 Важные детали

### Формат сообщений в состоянии:

```javascript
const message = {
  id: uniqueId(),
  type: 'user' | 'bot',
  timestamp: Date.now(),
  
  // Один из двух:
  text: 'Простой текст',
  // ИЛИ
  content: <ReactComponent />  // Для расписания
};
```

### Пример отображения расписания с fuzzy logic:

```jsx
<ScheduleCard lesson={lesson}>
  <div className="time">{lesson.time}</div>
  <div className="subject">{lesson.subject}</div>
  <div className="teacher">{lesson.teacher}</div>
  <div className="room">{lesson.room}</div>
  
  {/* Оценка нечёткой логики */}
  <div className="fuzzy-score" style={{color: getScoreColor(lesson.fuzzyEvaluation.score)}}>
    {lesson.fuzzyEvaluation.label}
    <span className="score">{lesson.fuzzyEvaluation.score}/10</span>
  </div>
  <div className="advice">{lesson.fuzzyEvaluation.advice}</div>
</ScheduleCard>
```

## 🚀 Шаги для старта

1. **Создать React приложение**
   ```bash
   npx create-react-app schedule-chatbot-ui
   # или
   npm create vite@latest schedule-chatbot-ui -- --template react
   ```

2. **Скопировать существующие файлы**
   - Перенести `src/services/`
   - Перенести `src/data/`

3. **Создать компоненты UI**
   - Начать с Chat.jsx
   - Затем MessageList и Message
   - Потом InputField
   - В конце ScheduleCard

4. **Интегрировать**
   - Подключить commandParser
   - Подключить scheduleService
   - Отобразить результаты

## ⚠️ Что НЕ нужно делать

- ❌ Не создавать backend (пока)
- ❌ Не менять логику fuzzyLogic.js (она работает!)
- ❌ Не усложнять — простой MVP достаточно
- ❌ Не добавлять авторизацию
- ❌ Не делать сохранение в localStorage (пока)

## 🎯 MVP критерии успеха

Проект готов, когда:

1. ✅ Можно ввести "расписание ПИ-301" и увидеть карточки занятий
2. ✅ Каждое занятие показывает оценку из нечёткой логики
3. ✅ Можно запросить расписание преподавателя
4. ✅ Команда "помощь" показывает список команд
5. ✅ Интерфейс выглядит прилично на мобильном и десктопе

## 📚 Полезные ссылки к файлам

- **Демо нечёткой логики:** `demo-standalone.js` (запустить: `node demo-standalone.js`)
- **Документация:** `README.md`
- **Шпаргалка:** `CHEATSHEET.md`

## 🔄 Следующие итерации (после MVP)

1. Добавить данные реального колледжа
2. Парсинг расписания с сайта
3. Backend (Node.js или Python)
4. База данных
5. Графы (кратчайший путь между аудиториями)

---

## 💡 Подсказки для Claude Code

- Используй существующий scheduleService — не переписывай его
- fuzzyLogic.js уже экспортирует FuzzyLogicEngine — используй его
- commandParser.parse() возвращает объект с type и параметрами
- scheduleData.js содержит реальную структуру данных — изучи её
- Все сервисы экспортируют синглтон (default export)

## 🎨 Приоритеты разработки

1. **Высокий:** Базовый функционал чата (ввод/вывод)
2. **Высокий:** Отображение расписания с оценками
3. **Средний:** Красивая стилизация
4. **Средний:** Адаптивность
5. **Низкий:** Анимации

---

**Статус проекта:** Логика готова ✅, нужен UI 🎨

**Последнее обновление:** 16 февраля 2026

**Версия документа:** 1.0
